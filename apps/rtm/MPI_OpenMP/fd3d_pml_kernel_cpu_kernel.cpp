//
// auto-generated by ops.py
//

//user function

// host stub function
#ifndef OPS_LAZY
void ops_par_loop_fd3d_pml_kernel(char const *name, ops_block block, int dim, int* range,
 ops_arg arg0, ops_arg arg1, ops_arg arg2, ops_arg arg3,
 ops_arg arg4) {
#else
void ops_par_loop_fd3d_pml_kernel_execute(ops_kernel_descriptor *desc) {
  ops_block block = desc->block;
  int dim = desc->dim;
  int *range = desc->range;
  ops_arg arg0 = desc->args[0];
  ops_arg arg1 = desc->args[1];
  ops_arg arg2 = desc->args[2];
  ops_arg arg3 = desc->args[3];
  ops_arg arg4 = desc->args[4];
  #endif

  //Timing
  double __t1,__t2,__c1,__c2;

  ops_arg args[5] = { arg0, arg1, arg2, arg3, arg4};



  #if defined(CHECKPOINTING) && !defined(OPS_LAZY)
  if (!ops_checkpointing_before(args,5,range,1)) return;
  #endif

  if (block->instance->OPS_diags > 1) {
    ops_timing_realloc(block->instance,1,"fd3d_pml_kernel");
    block->instance->OPS_kernels[1].count++;
    ops_timers_core(&__c2,&__t2);
  }

  #ifdef OPS_DEBUG
  ops_register_args(block->instance, args, "fd3d_pml_kernel");
  #endif


  //compute locally allocated range for the sub-block
  int start[3];
  int end[3];
  int arg_idx[3];
  #if defined(OPS_LAZY) || !defined(OPS_MPI)
  for ( int n=0; n<3; n++ ){
    start[n] = range[2*n];end[n] = range[2*n+1];
  }
  #else
  if (compute_ranges(args, 5,block, range, start, end, arg_idx) < 0) return;
  #endif

  #if defined(OPS_MPI)
  #if defined(OPS_LAZY)
  sub_block_list sb = OPS_sub_block_list[block->index];
  arg_idx[0] = sb->decomp_disp[0];
  arg_idx[1] = sb->decomp_disp[1];
  arg_idx[2] = sb->decomp_disp[2];
  #else
  arg_idx[0] -= start[0];
  arg_idx[1] -= start[1];
  arg_idx[2] -= start[2];
  #endif
  #else //OPS_MPI
  arg_idx[0] = 0;
  arg_idx[1] = 0;
  arg_idx[2] = 0;
  #endif //OPS_MPI

  //initialize global variable with the dimension of dats
  int xdim0_fd3d_pml_kernel = args[0].dat->size[0];
  int ydim0_fd3d_pml_kernel = args[0].dat->size[1];
  int xdim1_fd3d_pml_kernel = args[1].dat->size[0];
  int ydim1_fd3d_pml_kernel = args[1].dat->size[1];
  int xdim2_fd3d_pml_kernel = args[2].dat->size[0];
  int ydim2_fd3d_pml_kernel = args[2].dat->size[1];
  int zdim2_fd3d_pml_kernel = args[2].dat->size[2];
  int xdim3_fd3d_pml_kernel = args[3].dat->size[0];
  int ydim3_fd3d_pml_kernel = args[3].dat->size[1];
  int zdim3_fd3d_pml_kernel = args[3].dat->size[2];

  //set up initial pointers and exchange halos if necessary
  int base0 = args[0].dat->base_offset;
  float * __restrict__ rho_p = (float *)(args[0].data + base0);

  int base1 = args[1].dat->base_offset;
  float * __restrict__ mu_p = (float *)(args[1].data + base1);

  int base2 = args[2].dat->base_offset;
  float * __restrict__ yy_p = (float *)(args[2].data + base2);

  int base3 = args[3].dat->base_offset;
  float * __restrict__ dy_p = (float *)(args[3].data + base3);




  #ifndef OPS_LAZY
  //Halo Exchanges
  ops_H_D_exchanges_host(args, 5);
  ops_halo_exchanges(args,5,range);
  ops_H_D_exchanges_host(args, 5);
  #endif

  if (block->instance->OPS_diags > 1) {
    ops_timers_core(&__c1,&__t1);
    block->instance->OPS_kernels[1].mpi_time += __t1-__t2;
  }

  #pragma omp parallel for collapse(2)
  for ( int n_z=start[2]; n_z<end[2]; n_z++ ){
    for ( int n_y=start[1]; n_y<end[1]; n_y++ ){
      #pragma omp simd
      for ( int n_x=start[0]; n_x<end[0]; n_x++ ){
        int idx[] = {arg_idx[0]+n_x, arg_idx[1]+n_y, arg_idx[2]+n_z};
        const ACC<float> rho(xdim0_fd3d_pml_kernel, ydim0_fd3d_pml_kernel, rho_p + n_x*1 + n_y * xdim0_fd3d_pml_kernel*1 + n_z * xdim0_fd3d_pml_kernel * ydim0_fd3d_pml_kernel*1);
        const ACC<float> mu(xdim1_fd3d_pml_kernel, ydim1_fd3d_pml_kernel, mu_p + n_x*1 + n_y * xdim1_fd3d_pml_kernel*1 + n_z * xdim1_fd3d_pml_kernel * ydim1_fd3d_pml_kernel*1);
        #ifdef OPS_SOA
        const ACC<float> yy(6, xdim2_fd3d_pml_kernel, ydim2_fd3d_pml_kernel, zdim2_fd3d_pml_kernel, yy_p + n_x*1 + n_y * xdim2_fd3d_pml_kernel*1 + n_z * xdim2_fd3d_pml_kernel * ydim2_fd3d_pml_kernel*1);
        #else
        const ACC<float> yy(6, xdim2_fd3d_pml_kernel, ydim2_fd3d_pml_kernel, zdim2_fd3d_pml_kernel, yy_p + 6*(n_x*1 + n_y * xdim2_fd3d_pml_kernel*1 + n_z * xdim2_fd3d_pml_kernel * ydim2_fd3d_pml_kernel*1));
        #endif
        #ifdef OPS_SOA
        ACC<float> dy(6, xdim3_fd3d_pml_kernel, ydim3_fd3d_pml_kernel, zdim3_fd3d_pml_kernel, dy_p + n_x*1 + n_y * xdim3_fd3d_pml_kernel*1 + n_z * xdim3_fd3d_pml_kernel * ydim3_fd3d_pml_kernel*1);
        #else
        ACC<float> dy(6, xdim3_fd3d_pml_kernel, ydim3_fd3d_pml_kernel, zdim3_fd3d_pml_kernel, dy_p + 6*(n_x*1 + n_y * xdim3_fd3d_pml_kernel*1 + n_z * xdim3_fd3d_pml_kernel * ydim3_fd3d_pml_kernel*1));
        #endif
        
  float sigma = mu(0,0,0)/rho(0,0,0);
  float sigmax=0.0f;
  float sigmay=0.0f;
  float sigmaz=0.0f;

  if(idx[0]<=xbeg+pml_width){
    sigmax = (xbeg+pml_width-idx[0])*sigma/pml_width;
  }
  if(idx[0]>=xend-pml_width){
    sigmax=(idx[0]-(xend-pml_width))*sigma/pml_width;
  }
  if(idx[1]<=ybeg+pml_width){
    sigmay=(ybeg+pml_width-idx[1])*sigma/pml_width;
  }
  if(idx[1]>=yend-pml_width){
    sigmay=(idx[1]-(yend-pml_width))*sigma/pml_width;
  }
  if(idx[2]<=zbeg+pml_width){
    sigmaz=(zbeg+pml_width-idx[2])*sigma/pml_width;
  }
  if(idx[2]>=zend-pml_width){
    sigmaz=(idx[2]-(zend-pml_width))*sigma/pml_width;
  }





  float px = yy(0,0,0,0);
  float py = yy(1,0,0,0);
  float pz = yy(2,0,0,0);

  float vx = yy(3,0,0,0);
  float vy = yy(4,0,0,0);
  float vz = yy(5,0,0,0);

  float vxx=0.0f;
  float vyy=0.0f;
  float vzz=0.0f;

  float pxx=0.0f;
  float pxy=0.0f;
  float pxz=0.0f;

  float pyx=0.0f;
  float pyy=0.0f;
  float pyz=0.0f;

  float pzx=0.0f;
  float pzy=0.0f;
  float pzz=0.0f;

  for(int i=-halff;i<=halff;i++)
    {
#define CX
#ifndef CX
      pxx += yy(0,i,0,0)*c[i]*invdx;
      pyx += yy(1,i,0,0)*c[i]*invdx;
      pzx += yy(2,i,0,0)*c[i]*invdx;
      vxx += yy(3,i,0,0)*c[i]*invdx;

      pxy += yy(0,0,i,0)*c[i]*invdy;
      pyy += yy(1,0,i,0)*c[i]*invdy;
      pzy += yy(2,0,i,0)*c[i]*invdy;
      vyy += yy(4,0,i,0)*c[i]*invdy;

      pxz += yy(0,0,0,i)*c[i]*invdz;
      pyz += yy(1,0,0,i)*c[i]*invdz;
      pzz += yy(2,0,0,i)*c[i]*invdz;
      vzz += yy(5,0,0,i)*c[i]*invdz;

#else
      int j = i+halff;
      pxx += yy(0,i,0,0)*cx[j];
      pyx += yy(1,i,0,0)*cx[j];
      pzx += yy(2,i,0,0)*cx[j];
      vxx += yy(3,i,0,0)*cx[j];

      pxy += yy(0,0,i,0)*cy[j];
      pyy += yy(1,0,i,0)*cy[j];
      pzy += yy(2,0,i,0)*cy[j];
      vyy += yy(4,0,i,0)*cy[j];

      pxz += yy(0,0,0,i)*cz[j];
      pyz += yy(1,0,0,i)*cz[j];
      pzz += yy(2,0,0,i)*cz[j];
      vzz += yy(5,0,0,i)*cz[j];
#endif
    }

  dy(0,0,0,0)=vxx/rho(0,0,0) - sigmax*px;
  dy(1,0,0,0)=vyy/rho(0,0,0) - sigmay*py;
  dy(2,0,0,0)=vzz/rho(0,0,0) - sigmaz*pz;

  dy(3,0,0,0)=(pxx+pyx+pzx)*mu(0,0,0) - sigmax*vx;
  dy(4,0,0,0)=(pxy+pyy+pzy)*mu(0,0,0) - sigmay*vy;
  dy(5,0,0,0)=(pxz+pyz+pzz)*mu(0,0,0) - sigmaz*vz;


      }
    }
  }
  if (block->instance->OPS_diags > 1) {
    ops_timers_core(&__c2,&__t2);
    block->instance->OPS_kernels[1].time += __t2-__t1;
  }
  #ifndef OPS_LAZY
  ops_set_dirtybit_host(args, 5);
  ops_set_halo_dirtybit3(&args[3],range);
  #endif

  if (block->instance->OPS_diags > 1) {
    //Update kernel record
    ops_timers_core(&__c1,&__t1);
    block->instance->OPS_kernels[1].mpi_time += __t1-__t2;
    block->instance->OPS_kernels[1].transfer += ops_compute_transfer(dim, start, end, &arg0);
    block->instance->OPS_kernels[1].transfer += ops_compute_transfer(dim, start, end, &arg1);
    block->instance->OPS_kernels[1].transfer += ops_compute_transfer(dim, start, end, &arg2);
    block->instance->OPS_kernels[1].transfer += ops_compute_transfer(dim, start, end, &arg3);
  }
}


#ifdef OPS_LAZY
void ops_par_loop_fd3d_pml_kernel(char const *name, ops_block block, int dim, int* range,
 ops_arg arg0, ops_arg arg1, ops_arg arg2, ops_arg arg3,
 ops_arg arg4) {
  ops_kernel_descriptor *desc = (ops_kernel_descriptor *)calloc(1,sizeof(ops_kernel_descriptor));
  desc->name = name;
  desc->block = block;
  desc->dim = dim;
  desc->device = 0;
  desc->index = 1;
  desc->hash = 5381;
  desc->hash = ((desc->hash << 5) + desc->hash) + 1;
  for ( int i=0; i<6; i++ ){
    desc->range[i] = range[i];
    desc->orig_range[i] = range[i];
    desc->hash = ((desc->hash << 5) + desc->hash) + range[i];
  }
  desc->nargs = 5;
  desc->args = (ops_arg*)ops_malloc(5*sizeof(ops_arg));
  desc->args[0] = arg0;
  desc->hash = ((desc->hash << 5) + desc->hash) + arg0.dat->index;
  desc->args[1] = arg1;
  desc->hash = ((desc->hash << 5) + desc->hash) + arg1.dat->index;
  desc->args[2] = arg2;
  desc->hash = ((desc->hash << 5) + desc->hash) + arg2.dat->index;
  desc->args[3] = arg3;
  desc->hash = ((desc->hash << 5) + desc->hash) + arg3.dat->index;
  desc->args[4] = arg4;
  desc->function = ops_par_loop_fd3d_pml_kernel_execute;
  if (block->instance->OPS_diags > 1) {
    ops_timing_realloc(block->instance,1,"fd3d_pml_kernel");
  }
  ops_enqueue_kernel(desc);
}
#endif
